type Query {
    sayHello: String
    printHello: String
    sayHelloTo(name: String!): String
    random: Int!
    customers: [Customer]
    customerById(id: ID!): Customer
    customersNameContains(name: String!): [Customer] @deprecated(reason: "being deprecated")
    customersByAgeRange(filter: AgeRangeFilter!): [Customer]!
    users: [User]
}

input AgeRangeFilter {
    minAge: Int!
    maxAge: Int!
}

type Customer {
    id: ID
    name: String
    age: Int
    city: String
}